import { Body, Controller, Get, Param, ParseUUIDPipe, Patch, Post, Query, UseGuards } from '@nestjs/common'; import { AgentRole } from '../agents/agent.entity'; import { Roles } from '../auth/decorators/roles.decorator'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { RolesGuard } from '../auth/guards/roles.guard'; import { CreatePaymentDto, PaymentQueryDto } from './payment.dto'; import { PaymentsService } from './payments.service';
@Controller('admin/finance/payments') @UseGuards(JwtAuthGuard,RolesGuard) @Roles(AgentRole.ADMIN) export class PaymentsController { constructor(private readonly payments:PaymentsService){} @Get() list(@Query() q:PaymentQueryDto){return this.payments.list(q);} @Post() create(@Body() dto:CreatePaymentDto){return this.payments.create(dto);} @Patch(':id/payout') payout(@Param('id',new ParseUUIDPipe({version:'4'})) id:string){return this.payments.payout(id);} }
