import { MigrationInterface, QueryRunner } from 'typeorm';
export class CreateAcquisitionCommissions1789344000000 implements MigrationInterface {
  name = 'CreateAcquisitionCommissions1789344000000';
  async up(q: QueryRunner): Promise<void> { await q.query(`CREATE TYPE "CommissionInstallmentStatus" AS ENUM ('PENDING','PAID')`); await q.query(`CREATE TABLE acquisition_commissions (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), lease_id uuid NOT NULL UNIQUE REFERENCES leases(id) ON DELETE RESTRICT, total_amount numeric(12,2) NOT NULL CHECK (total_amount > 0), installment_count integer NOT NULL CHECK (installment_count BETWEEN 1 AND 60), notes text NOT NULL, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now())`); await q.query(`CREATE TABLE commission_installments (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), commission_id uuid NOT NULL REFERENCES acquisition_commissions(id) ON DELETE CASCADE, installment_number integer NOT NULL, due_date date NOT NULL, amount numeric(12,2) NOT NULL CHECK (amount > 0), status "CommissionInstallmentStatus" NOT NULL DEFAULT 'PENDING', paid_at timestamptz NULL, payment_note text NULL, UNIQUE (commission_id, installment_number))`); }
  async down(q: QueryRunner): Promise<void> { await q.query('DROP TABLE commission_installments'); await q.query('DROP TABLE acquisition_commissions'); await q.query('DROP TYPE "CommissionInstallmentStatus"'); }
}
