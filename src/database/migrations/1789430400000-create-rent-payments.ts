import { MigrationInterface, QueryRunner } from 'typeorm'; export class CreateRentPayments1789430400000 implements MigrationInterface { name='CreateRentPayments1789430400000'; async up(q:QueryRunner){await q.query(`CREATE TYPE "RentPaymentStatus" AS ENUM ('RECEIVED','PAID_OUT')`);await q.query(`CREATE TABLE rent_payments (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), lease_id uuid NOT NULL REFERENCES leases(id) ON DELETE RESTRICT, reference_month date NOT NULL, received_amount numeric(12,2) NOT NULL CHECK (received_amount > 0), commission_amount numeric(12,2) NOT NULL DEFAULT 0, net_amount numeric(12,2) NOT NULL, status "RentPaymentStatus" NOT NULL DEFAULT 'RECEIVED', received_at timestamptz NOT NULL, paid_out_at timestamptz NULL, payment_note text NULL, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now(), UNIQUE (lease_id, reference_month))`);await q.query('CREATE INDEX "IDX_rent_payments_month" ON rent_payments (reference_month)');} async down(q:QueryRunner){await q.query('DROP TABLE rent_payments');await q.query('DROP TYPE "RentPaymentStatus"');}}
